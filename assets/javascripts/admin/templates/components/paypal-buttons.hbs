{{#if items}}
	<div class='items'>
		{{#each item in items}}
			<div class='item'>
				<div class='row-1'>
					{{input type='text' autoresize=true value=item.id}}
					{{d-button
						action='removeItem'
						actionParam=item
						icon='times'
						class='btn-small no-text'
						title='admin.site_settings.paypal_buy_now.delete'
					}}
				</div>
				{{textarea value=item.html}}
			</div>
		{{/each}}
	</div>
{{/if}}
<div class='item item-new'>
	<div class='row-1'>
		{{input value=newId autoresize=true}}
		{{d-button
			action='addItem'
			icon='plus'
			class='btn-primary btn-small no-text'
			disabled=inputInvalid
			title='admin.site_settings.paypal_buy_now.add'
		}}
	</div>
	{{textarea value=newHtml}}
</div>
